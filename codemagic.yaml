workflows:
  react-native:
    name: DynaFood App
    environment:
      node: latest
    scripts:
      - npm install -g expo-cli
      - npm install --legacy-peer-deps
      - npm test
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: develop
          include: true
          source: true